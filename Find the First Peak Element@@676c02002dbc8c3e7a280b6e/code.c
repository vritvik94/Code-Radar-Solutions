#include<stdio.h>

int main(){
    int i,n,max;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    scanf("%d",&n);
    max=a[0];
    if(max>a[i])
        max=a[i];
    else
        printf("-1");
    printf("%d",a[i]);
    return 0;
}