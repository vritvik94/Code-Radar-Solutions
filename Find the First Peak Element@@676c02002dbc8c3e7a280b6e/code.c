#include<stdio.h>

int main(){
    int i,n,max;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    scanf("%d",&n);
    max=a[0];
    if(max>a[i])
        printf("%d",a[i]);
    return 0;
}